/**
 * Feature-local **grid draft** surface: persistable draft types, compare/save helpers, and (in
 * `./geometry/`) overlay adapters for the map editor — not reusable domain editor semantics.
 *
 * For editor rules (modes, select-mode, placement, palettes), use `domain/authoring/editor/`.
 */
export * from './draft/locationGridDraft.types';
export * from './draft/locationGridDraft.utils';
