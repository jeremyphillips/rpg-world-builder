/** Dimming when the row is explicitly disabled (e.g. spell pickers). */
export const COMPACT_ACTION_DISABLED_OPACITY = 0.4

/** Dimming when the action exists but is not currently available (e.g. encounter). */
export const COMPACT_ACTION_UNAVAILABLE_OPACITY = 0.5

export const COMPACT_ACTION_OPACITY_TRANSITION = 'opacity 0.15s ease'
